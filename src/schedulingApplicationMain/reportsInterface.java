package schedulingApplicationMain;

public interface reportsInterface {
    /**Lambda expression #1. This lambda is used to increment the total appointments associated with a specific type and is used as an effective counter. */
    int newTotal(int currentTotal);

}
